/**
 * @file
 * Contains the block api.
 */

// Load dependencies.
const fs = require('fs-extra');
const path = require('path');

/**
 * @class
 * @classdesc Constructs the block class.
 */
class Block {

  /**
   * Constructs block class properties.
   */
  constructor(packageJson = {}, resources = {}, instances = {}) {

  }
}

module.exports = Block;
